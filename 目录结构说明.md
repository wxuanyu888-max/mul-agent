# Mul-Agent 目录结构说明

## 目录结构概览

```
mul-agent/
├── wang/                          # Agent 配置主目录（优先使用）
│   ├── agent-team/                # Agent 团队配置
│   │   ├── {agent_id}/            # 每个 Agent 的独立目录
│   │   │   ├── user.md            # Agent 角色和职责定义
│   │   │   ├── soul.md            # Agent 核心特质和行为模式
│   │   │   ├── skill.md           # Agent 技能和能力定义
│   │   │   ├── memory.md          # Agent 记忆配置
│   │   │   └── logic.md           # Agent 逻辑和路由规则
│   │   └── .templates/            # 配置模板
│   │       └── logic.md           # Logic 配置模板
│   ├── settings.json              # 全局设置
│   ├── QUICKSTART.md              # 快速开始指南
│   ├── README.md                  # Wang 目录说明
│   ├── hooks/                     # Hooks 配置
│   └── mcp-configs/               # MCP 服务器配置
│
├── storage/                       # 数据存储目录
│   ├── agents/                    # 旧配置存储（回退用）
│   ├── token_usage/               # Token 使用统计（独立存储）
│   │   ├── {agent_id}.json        # JSON 格式统计数据
│   │   └── {agent_id}.md          # Markdown 格式（已废弃）
│   ├── snapshots/                 # 配置快照
│   └── projects/                  # 项目配置
│
└── mul_agent/                     # 核心代码
    ├── api/                       # API 路由
    ├── brain/                     # 大脑/决策模块
    │   ├── config_manager.py      # 配置管理器
    │   ├── token_usage.py         # Token 使用统计
    │   ├── handlers/              # 请求处理器
    │   └── ...
    ├── memory/                    # 记忆模块
    └── common/                    # 公共模块
```

## Wang/Agent-Team 目录规范

### 每个 Agent 只允许包含 5 个文件

| 文件名 | 用途 | 是否必需 |
|--------|------|----------|
| `user.md` | Agent 角色、职责、能力定义 | 是 |
| `soul.md` | Agent 核心特质、行为模式、进化规则 | 是 |
| `skill.md` | Agent 技能、工具使用能力 | 是 |
| `memory.md` | Agent 记忆系统配置 | 是 |
| `logic.md` | Agent 逻辑规则、路由决策、工具权限 | 是 |

### 不允许在 wang/agent-team 中包含的文件

- ❌ `token_usage.md` - 已移至 `storage/token_usage/`
- ❌ `token_usage.json` - 已移至 `storage/token_usage/`
- ❌ `prompt.md` - 可选，仅特殊 Agent 需要（如 wangyue）
- ❌ 其他任意文件

## Storage/Token_Usage 目录

### 用途
存储所有 Agent 的 Token 使用统计，与 wang/agent-team 分离。

### 文件结构
```
storage/token_usage/
├── {agent_id}.json    # JSON 格式，包含完整统计数据
└── {agent_id}.md      # Markdown 格式（已废弃，仅保留向后兼容）
```

### 数据结构
```json
{
  "version": "1.0",
  "agent_id": "wangyue",
  "created_at": "2026-03-07T08:42:18.282596",
  "updated_at": "2026-03-07T08:42:18.282599",
  "last_access_time": null,
  "totals": {
    "input_tokens": 0,
    "output_tokens": 0,
    "total_tokens": 0,
    "access_count": 0
  },
  "by_model": {},
  "by_function": {},
  "by_date": {},
  "llm_logs": []
}
```

## ConfigManager API

### 核心配置操作
```python
# 加载配置
config = config_manager.load(agent_id, "user")    # 加载 user.md
config = config_manager.load(agent_id, "soul")    # 加载 soul.md
config = config_manager.load(agent_id, "skill")   # 加载 skill.md
config = config_manager.load(agent_id, "memory")  # 加载 memory.md

# 保存配置
config_manager.save(agent_id, "user", data)
```

### Token 使用统计操作
```python
# 加载 Token 使用统计
usage = config_manager.load_token_usage(agent_id)

# 保存 Token 使用统计
config_manager.save_token_usage(agent_id, data)
```

### Logic 配置操作
```python
# 加载 Logic 配置
logic = config_manager.load_logic(agent_id)

# 保存 Logic 配置
config_manager.save_logic(agent_id, data)
```

## 文件内容说明

### user.md
定义 Agent 的角色、职责和能力：
- 角色类型（coordinator, coder, tester 等）
- 职责列表
- 能力范围
- LLM 配置
- 工具权限

### soul.md
定义 Agent 的核心特质：
- 个性特征
- 价值观
- 目标
- 行为模式
- 进化规则
- 约束条件

### skill.md
定义 Agent 的技能：
- 技能列表
- 每个技能的详细描述
- 使用场景
- 执行步骤

### memory.md
定义记忆系统配置：
- 短期记忆配置
- 长期记忆配置
- 交接记忆配置
- 记忆保留规则

### logic.md
定义逻辑和路由规则：
- 可用动作列表
- 路由决策逻辑
- 工具权限
- 行为规则
- 进化规则

## 迁移指南

### 从旧结构迁移

1. **Token Usage 文件迁移**
   ```bash
   # 自动迁移（已完成）
   mv wang/agent-team/{agent}/token_usage.md storage/token_usage/{agent}.md
   mv wang/agent-team/{agent}/token_usage.json storage/token_usage/{agent}.json
   ```

2. **Logic.md 创建**
   ```bash
   # 自动创建（已完成）
   # 为每个 Agent 创建 logic.md 文件
   ```

3. **代码更新**
   - `config_manager.py` - 添加 `load_token_usage()`, `save_token_usage()`, `load_logic()`, `save_logic()` 方法
   - `token_usage.py` - 更新 `_load_usage()` 和 `_save_usage()` 使用新目录

## 最佳实践

1. **配置分离**
   - 核心配置（user/soul/skill/memory/logic）放在 `wang/agent-team/`
   - 统计数据（token_usage）放在 `storage/token_usage/`

2. **只读统计**
   - Token 使用统计不应该在 wang/agent-team 中显示
   - 所有统计操作通过 `storage/token_usage/` 进行

3. **模板使用**
   - 创建新 Agent 时使用 `.templates/` 中的模板
   - 保持配置一致性

4. **版本控制**
   - 所有配置文件包含 `version` 字段
   - 修改配置时更新版本号
