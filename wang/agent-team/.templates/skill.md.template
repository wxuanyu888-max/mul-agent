---
version: "1.0"
agent_id: ${agent_id}
---

# Skill Tree - 渐进式能力披露

## 第一层：基础能力 (新手用户看到)

这些能力会自动触发，用户无需知道命令格式：

| 意图模式 | 自动触发能力 | 示例输入 |
|---------|-------------|---------|
| 问候 | 友好响应 | "你好", "hi" |
| 问题 | 直接回答 | "这个项目怎么样" |
| 命令格式 | bash 执行 | "ls -la", "$ cat file" |
| 求助 | 帮助菜单 | "help", "帮助" |

## 第二层：协作能力 (进阶用户看到)

当用户表现出协作需求时披露：

| 意图模式 | 能力 | 披露时机 |
|---------|------|---------|
| 创建类词汇 | create_user | 用户说"创建/新建"时 |
| 团队类词汇 | create_team | 用户提到"团队/组队"时 |
| 记忆类词汇 | memory | 用户提到"记住/记忆"时 |
| 对话类词汇 | chat | 用户提到"和 xx 对话"时 |

## 第三层：专家能力 (高级用户看到)

当用户明确请求或系统需要时披露：

| 能力 | 触发条件 | 示例 |
|------|---------|------|
| heart (自省) | 用户明确输入"heart/evolve" | "heart" |
| network_delegate | 任务复杂度超过阈值 | 自动委派 |
| network_broadcast | 需要多 agent 协作 | 自动广播 |
| token_usage | 用户查询使用情况 | "查看 token 使用" |

## 路由决策树

```
用户输入
    │
    ├── 简单交互 (问候、问题) → route: response
    │
    ├── 明确命令格式 → route: bash
    │
    ├── 包含协作关键词 → route: create_user/chat/memory
    │
    ├── 明确高级命令 → route: heart/network_*
    │
    └── 无法识别 → route: uncertain → LLM 分析意图
```

## 能力解锁条件

```yaml
level_1_unlocked: always
level_2_unlocked:
  conditions:
    - user_has_created_agent: true
    - OR conversation_length > 10
level_3_unlocked:
  conditions:
    - user_explicitly_requests: true
    - OR agent_complexity_score > 0.7
```
