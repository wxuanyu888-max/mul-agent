---
version: "2.0"
agent_id: ${agent_id}
---

# Agent Load Configuration

## 默认加载文件列表

系统初始化时按顺序加载以下文件（支持相对路径和绝对路径）：

```yaml
files:
  - path: soul.md           # 相对路径：当前 agent 目录
    required: true
  - path: user.md
    required: true
  - path: logic.md
    required: true
  - path: skill.md
    required: true
  - path: memory.md
    required: true
  - path: prompt.md
    required: false
```

## 路径说明

| 路径类型 | 示例 | 说明 |
|---------|------|------|
| 相对路径 | `soul.md` | 相对于 `agent-team/{agent_id}/` 目录 |
| 绝对路径 | `/Users/agent/.../prompt.md` | 完整的文件系统路径 |
| 模板路径 | `.templates/xxx.md` | 相对于 `agent-team/` 目录 |

## 加载规则

1. 系统仅加载本列表配置的文件
2. 按列表顺序依次加载
3. `required: false` 的文件，不存在时跳过

---
